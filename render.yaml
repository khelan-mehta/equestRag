services:
  - type: web
    name: equest-backend
    env: python
    plan: free

    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: |
      gunicorn app:app --bind 0.0.0.0:$PORT --worker-tmp-dir /dev/shm

    envVars:
      - key: PYTHON_VERSION
        value: 3.10.8

    healthCheckPath: /api/health
    autoDeploy: true
